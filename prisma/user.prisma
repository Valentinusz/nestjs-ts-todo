model User {
  id              String           @id @db.VarChar(26)
  idpId           String
  tasks           Task[]
  taskCollections TaskCollection[]
  createdAt       DateTime         @default(now())
  deletedAt       DateTime?
}
