// https://pris.ly/d/prisma-schema
generator client {
  provider = "prisma-client-js"
  output   = "../generated/prisma/tasks"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
